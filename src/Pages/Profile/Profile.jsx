import { Helmet } from "react-helmet-async";

const Profile = () => {
    return (
        <div>
            <Helmet>
                <title>Roam Rift || Profile</title>
            </Helmet>
            Profile page
        </div>
    );
};

export default Profile;