import { Helmet } from "react-helmet-async";

const Register = () => {
    return (
        <div>
            <Helmet>
                <title>Roam Rift || SignUp</title>
            </Helmet>
            Register page
        </div>
    );
};

export default Register;