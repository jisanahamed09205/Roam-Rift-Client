import { Helmet } from "react-helmet-async";

const WishList = () => {
    return (
        <div>
            <Helmet>
                <title>Roam Rift || Wish List</title>
            </Helmet>
        </div>
    );
};

export default WishList;