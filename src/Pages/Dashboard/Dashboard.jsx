import { Helmet } from "react-helmet-async";

const Dashboard = () => {
    return (
        <div>
            <Helmet>
                <title>Roam Rift || Dashboard</title>
            </Helmet>
            Dashboard
        </div>
    );
};

export default Dashboard;