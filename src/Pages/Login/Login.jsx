import { Helmet } from "react-helmet-async";

const Login = () => {
    return (
        <div>
            <Helmet>
                <title>Roam Rift || Login</title>
            </Helmet>
            Login page
        </div>
    );
};

export default Login;